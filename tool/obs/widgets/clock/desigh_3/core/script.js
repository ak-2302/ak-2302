const params=new URLSearchParams(location.search);const root=document.documentElement;root.style.setProperty("--color",params.get("color")||"#f6f7f1");root.style.setProperty("--accent",params.get("accent")||"#ff6b6b");root.style.setProperty("--bg",params.get("bg")||"#151618");root.style.setProperty("--shadow",`${params.get("shadow")||10}px`);const outline=params.get("outline")||0;root.style.setProperty("--stroke",`0 0 ${outline}px #000`);const locale=params.get("lang")==="en"?"en-US":"ja-JP";function tick(){const now=new Date();document.querySelector("#weekday").textContent=now.toLocaleDateString(locale,{weekday:"short"}).toUpperCase();document.querySelector("#date").textContent=now.toLocaleDateString("ja-JP",{month:"2-digit",day:"2-digit"}).replace("/" ,".");document.querySelector("#time").textContent=now.toLocaleTimeString("ja-JP",{hour:"2-digit",minute:"2-digit",hour12:false})}tick();setInterval(tick,1000);

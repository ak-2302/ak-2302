# トップページ改善点

対象: `index.html`、`ref/style/style.css`、`ref/style/3d.css`、`ref/script/3d.js`、`ref/script/script.js`

## 現状

トップページは、物理演算で動く4つの球をプロフィール・ツール・ソーシャルリンク・コンタクトへの主要ナビゲーションとして利用している。視覚的な独自性は高く、暗い背景と白い球、ライム色のアクセントによってポートフォリオの世界観も統一されている。

一方で、サイトの主要情報と導線が3D表示とモーダルに集約されているため、初見ユーザーや3Dを利用できない環境では「何ができるサイトか」「どこを押せばよいか」が伝わりにくい。改善では、現在の表現を残しながら、理解しやすさ・操作の確実性・代替導線を補強することを優先する。

## 優先度A: 先に対応したい項目

### 1. 3Dに依存しないナビゲーションを追加する

現状は球をクリックしないと4つのコンテンツに到達できない。`noscript`の案内はあるが、WebGL非対応、ライブラリ読み込み失敗、支援技術利用時の代替操作にはなっていない。

対応案:

- 3Dステージの下または横に、`PROFILE`、`TOOL`、`LINK`、`CONTACT`のテキストリンクまたはボタンを常時表示する。
- 代替ナビゲーションからも既存のモーダルを開けるよう、開閉処理を共通化する。
- WebGL/Physijsの初期化失敗時は、エラー文だけでなく代替ナビゲーションを表示する。

確認方法:

- JavaScript無効時、ライブラリ読み込み失敗時、キーボード操作時でも4項目に到達できる。

### 2. 4つの球の意味と操作方法を画面内で明確にする

現在の説明は「4つの球が、それぞれの入り口です」に留まり、球上のラベルを読めない状態や、クリックとドラッグの違いが分かりにくい可能性がある。`SELECT A SPHERE`という表示も、球がメニューであることを十分に説明していない。

対応案:

- 「球をクリックして開く」「空白部分をドラッグして回転」「ホイール/ピンチで拡大縮小」を短い日本語で表示する。
- 4項目の名前と役割を一覧で示す（例: `PROFILE — 経歴`、`TOOL — Webツール`）。
- 初回表示時だけ操作ヒントを強調し、操作後は控えめにする。

確認方法:

- 初見ユーザーが説明を読んで、各操作と4つの導線を迷わず説明できる。

### 3. キーボード操作とモーダルのフォーカス管理を改善する

`canvas`は`role="img"`であり、球ごとのキーボード操作がない。モーダルを開いた後も、Escapeで閉じる処理はあるが、フォーカストラップがなく、Tabキーで背後のページへ移動できる。また、閉じる際に元の要素へ戻れないケースも考えられる。

対応案:

- 3Dとは別に、キーボードで操作できる4つのボタンを提供する。
- モーダル表示中はTab/Shift+Tabをモーダル内に閉じ込める。
- モーダルのタイトル、説明、送信結果を支援技術が読み上げられるよう、`aria-describedby`やライブリージョンを整える。
- フォーカス時のアウトラインを明示し、マウスホバーだけに依存しない。

確認方法:

- マウスを使わず、Tab、Enter、Escapeだけで全導線を操作できる。

### 4. モーションを確実に抑制する

CSSには`prefers-reduced-motion`対応があるが、物理演算・`requestAnimationFrame`・球への初期インパルスは継続する。動きに配慮が必要なユーザー向けには、アニメーションを止める選択肢が必要である。

対応案:

- `prefers-reduced-motion: reduce`を検知した場合、物理シミュレーションと自動的な初期衝撃を停止する。
- 「動きを停止」トグルを用意し、ユーザーがいつでも静止表示へ切り替えられるようにする。
- `visibilitychange`だけでなく、ページ離脱時にも描画ループを整理する。

確認方法:

- OSの視差効果低減設定時に球が動き続けない。

## 優先度B: 体験品質を高める項目

### 5. ファーストビューにポートフォリオの要約を追加する

`index.html`には説明文があるが、誰が何を作っているかを一目で伝える見出しがない。検索結果やSNS共有時にも、現在のdescriptionだけでは人物・活動内容の要約が弱い。

対応案:

- `h1`を追加し、「Web開発者のポートフォリオ」など役割が分かる短い見出しを置く。
- 自己紹介を1文で補い、プロフィールを開かなくてもサイトの目的が分かるようにする。
- `og:title`、`og:description`、`og:image`、canonical URLを設定する。

### 6. 小さい文字と低コントラストを見直す

ヘッダー、フッター、操作ヒントには7〜9px程度の文字と暗いグレーが多い。雰囲気は保てるが、可読性とコントラストが不足する可能性がある。

対応案:

- 補助情報を最低でも読みやすいサイズに引き上げる。
- `--gray`以下の色をコントラスト検証し、重要な説明文は明るくする。
- 文字を小さくする代わりに、情報量を減らして余白を保つ。

### 7. モバイル表示で操作ヒントとフッター情報を整理する

画面幅に応じたCSSはあるが、420px以下ではズーム案内と一部の表示が隠れる。小画面ほど操作説明が重要になるため、非表示にする情報の優先順位を再検討する必要がある。

対応案:

- モバイルでは「タップで開く」を優先表示する。
- フッターの時刻や技術スタックより、ナビゲーションとサイトの目的を優先する。
- 縦長端末、横向き端末、ブラウザのアドレスバー表示変化でステージが切れないか確認する。

### 8. ローディング・エラー状態に次の行動を示す

初期化失敗時の`3D CONTENT COULD NOT LOAD`は原因と次の行動を示していない。通信環境やブラウザによってはユーザーが画面から進めなくなる。

対応案:

- エラー時に「下のメニューから閲覧できます」「再読み込み」などの行動を提示する。
- 初期化が長引く場合は、一定時間後に代替ナビゲーションを表示する。
- `aria-live`でローディング完了・失敗を通知する。

## 優先度C: 保守性・信頼性の改善

### 9. 外部フォント依存とライブラリ読み込みを見直す

CSSでGoogle Fontsを読み込み、Three.js/Physijsは個別のvendorファイルに依存している。外部フォントの読み込み遅延や、ライブラリのロード失敗が初期表示に影響する。

対応案:

- フォント未読込時でもレイアウトが崩れないよう、フォールバックと表示幅を確認する。
- 必要に応じてフォントをローカル配信し、CSPやネットワークエラー時の挙動を確認する。
- vendorファイルのバージョン・ライセンス・更新手順をREADMEまたは別ドキュメントに記載する。

### 10. お問い合わせ送信の失敗時UXを強化する

送信処理は失敗時の文言を表示するが、通信エラー、入力エラー、サーバーエラーの区別がない。また、送信中のボタン文言が変わらないため、操作が受け付けられたか分かりにくい。

対応案:

- 送信中はボタンを「送信中…」に変更し、完了・失敗時に元へ戻す。
- サーバーエラー時も入力内容を保持する。
- 入力欄ごとのエラー表示と、送信結果のライブリージョンを追加する。
- エンドポイントの可用性、レート制限、スパム対策を確認する。

## 推奨する実装順

1. 代替ナビゲーションとキーボード操作を追加する。
2. 4項目の意味と操作方法を日本語で補足する。
3. WebGL失敗時・ローディング中の代替導線を整える。
4. reduced motion対応とモーダルのフォーカス管理を実装する。
5. 見出し・メタ情報・コントラスト・モバイル表示を調整する。
6. お問い合わせ送信と外部依存の信頼性を確認する。

## 完了条件

- 3Dが使えなくても、4つのコンテンツを閲覧できる。
- キーボードだけで、トップページからモーダルの開閉・フォーム送信まで操作できる。
- 初見ユーザーがサイトの目的と4つの導線を理解できる。
- reduced motion設定時に自動的な物理演算・不要なアニメーションが停止する。
- デスクトップ・モバイル・WebGL非対応環境で、エラーや情報欠落がない。

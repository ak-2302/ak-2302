const params=new URLSearchParams(location.search);const root=document.documentElement;root.style.setProperty("--color",params.get("color")||"#fff");root.style.setProperty("--accent",params.get("accent")||"#bb86fc");root.style.setProperty("--bg",params.get("bg")||"#15101a");root.style.setProperty("--shadow",`${params.get("shadow")||10}px`);root.style.setProperty("--stroke",`0 0 ${params.get("outline")||0}px #000`);const lines=(params.get("items")||"21:00|START\n21:10|TALK\n22:00|END").split("\n").filter(Boolean);const list=document.querySelector("#items");for(const line of lines){const [time,...text]=line.split("|");const item=document.createElement("li");const timeNode=document.createElement("time");const textNode=document.createElement("span");timeNode.textContent=time;textNode.textContent=text.join("|");item.append(timeNode,textNode);list.append(item)}

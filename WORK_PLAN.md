# 個人ホームページ 作業計画書

参考：README.md、REQUIREMENTS.md、TECH_STACK.md、Webデザイン指針、Webデザイン詳細指針

## 1. 作業概要

`REQUIREMENTS.md`に定義された個人ホームページを、React + Vite + TypeScriptで段階的に実装する。

最終成果物は、作品・プロフィール紹介を主目的とした「1ページ＋作品詳細ページ」の静的サイトとする。2Dモーションを中心に、作品内容を補助する控えめな3D表現を加える。

既存リポジトリには大規模な削除変更が存在するため、それらを復元・上書きせず、現在の作業ツリーを起点に必要なファイルだけを追加・変更する。

## 2. 完了条件

以下をすべて満たした状態を作業完了とする。

- ホームと作品詳細ページをTypeScriptで実装できる。
- 作品データの追加・変更だけで作品一覧と詳細ページを更新できる。
- ページ内ナビゲーション、作品遷移、外部リンクが動作する。
- GSAPによる初期表示、スクロール、ホバー演出が動作する。
- reduced motion時にモーションが縮小される。
- 3D非対応時のフォールバックが表示される。
- 問い合わせフォームが入力検証、送信中、成功、失敗状態を持つ。
- TypeScript、Lint、単体テスト、ビルドが成功する。
- デスクトップ、タブレット、モバイルで主要導線が利用できる。
- GitHub Pagesへデプロイ可能である。

## 3. 実装フェーズ

### Phase 0：実装前確認

目的：不足情報と既存環境を確定し、後戻りを減らす。

作業内容：

- `package.json`、Vite設定、既存Worker、GitHub Actions、CNAMEを確認する。
- 既存のユーザー変更を一覧化し、作業対象外として保護する。
- 表示名、プロフィール本文、作品一覧、公開URL、画像、SNS URLを確定する。
- 問い合わせWorkerの本番URL、許可オリジン、通知先、保存方針を確定する。
- 作品データの最初のサンプルを最低3件準備する。

成果物：

- 実装対象ファイル一覧
- コンテンツ確定表
- 外部URL・API設定一覧

完了条件：実装に必要な外部URLと公開コンテンツが未確定のまま残っていない。

### Phase 1：プロジェクト基盤の再構成

目的：TypeScriptで安全に拡張できる最小構成を作る。

作業内容：

- React + Vite構成をTypeScriptへ移行する。
- `src/app`、`src/components`、`src/sections`、`src/data`、`src/styles`を作成する。
- `App.tsx`とルーティングの入口を作成する。
- `tokens.css`と`globals.css`を作成する。
- 型検査、Lint、テスト、ビルドのnpm scriptsを整備する。
- GitHub Pagesのベースパスと静的配信設定を確認する。

主な成果物：

- `src/app/App.tsx`
- `src/app/routes.tsx`
- `src/styles/tokens.css`
- `src/styles/globals.css`
- `tsconfig.json`
- `package.json`

完了条件：空のホーム画面をTypeScriptで起動、テスト、ビルドできる。

### Phase 2：コンテンツモデルと共通レイアウト

目的：作品データを中心に、重複しないページ構造を作る。

作業内容：

- `Work`型を定義する。
- プロフィール、外部リンク、作品データをTypeScriptファイルへ分離する。
- 作品の必須項目と任意項目を型で表現する。
- サイト共通のヘッダー、ナビゲーション、フッター、リンク、ボタンを実装する。
- 外部リンクの`target`と`rel`を共通ルールにする。
- 404相当の作品詳細表示を作る。

主な成果物：

- `src/data/profile.ts`
- `src/data/works.ts`
- `src/components/layout/*`
- `src/components/navigation/*`
- `src/components/media/*`

完了条件：作品データを1件追加するだけで一覧に表示でき、slugから詳細を取得できる。

### Phase 3：ホームページの情報構造

目的：モーションを追加する前に、読みやすく操作可能な静的UIを完成させる。

実装順：

1. ファーストビュー
2. プロフィール
3. 作品一覧
4. 実験・遊び
5. 外部リンク
6. 問い合わせ導線
7. フッター

作業内容：

- セマンティックHTMLで各セクションを実装する。
- ファーストビューに活動内容と主要CTAを置く。
- 作品一覧は均等カード列に固定せず、作品の重要度と内容に応じた編集的レイアウトにする。
- 作品ごとに異なる視覚的な仕掛けを用意するが、情報の可読性を優先する。
- 画像の代替テキストと読み込みサイズを設定する。
- モバイル幅で情報の順序と主要操作を維持する。

完了条件：JavaScriptアニメーションがなくても、主要情報・リンク・フォーム導線が成立する。

### Phase 4：作品詳細ページ

目的：作品の背景と成果物を再利用可能なテンプレートで提示する。

作業内容：

- `/:slug`または`/works/:slug`のルートを実装する。
- タイトル、概要、制作背景、技術、メディア、デモ、ソースコードを表示する。
- 任意項目がない作品でも余白や順序が崩れないようにする。
- 画像、動画、3Dプレビューを共通メディア領域から表示する。
- ホーム、作品一覧、関連作品への戻り導線を実装する。
- ページ固有のtitle、description、OGP情報を設定する。

完了条件：3件以上の異なる作品データで同じテンプレートが成立する。

### Phase 5：2Dモーションの実装

目的：情報構造を補助する滑らかな動きを追加する。

実装順：

1. 初回表示タイムライン
2. セクションのスクロール連動
3. 作品画像の視差・変形
4. 作品ホバー演出
5. ページ遷移

作業ルール：

- `@gsap/react`の`useGSAP`を使用する。
- ScrollTriggerはページ上部から順に作成する。
- 破棄時にGSAPコンテキストとScrollTriggerをクリーンアップする。
- 重要な操作をアニメーション完了待ちにしない。
- モバイルではマウス追従を無効化または簡略化する。
- `prefers-reduced-motion`では移動量・視差・継続時間を縮小する。
- Lenisは導入せず、ネイティブスクロールを使う。

完了条件：動きが情報の発見や操作を補助し、装飾だけの常時ループがない。

### Phase 6：控えめな3Dとフォールバック

目的：ヒーローまたは作品表示の一部に限定して3D表現を追加する。

作業内容：

- React Three FiberのCanvasを独立コンポーネントにする。
- 3D要素を遅延読み込みする。
- ユーザー操作またはスクロールに反応する表現に限定する。
- WebGL非対応、Canvas初期化失敗、低性能環境のフォールバックを用意する。
- 3Dを無効にしてもタイトル、本文、リンクを表示する。
- 装飾Canvasには適切なアクセシビリティ属性を設定する。

完了条件：3Dが利用できない環境でも、サイトの目的と主要操作が損なわれない。

### Phase 7：問い合わせフォームとWorker連携

目的：訪問者が安全に問い合わせを送信できるようにする。

フロントエンド作業：

- 名前、メールアドレス、メッセージ、同意確認を実装する。
- 必須入力、メール形式、文字数を検証する。
- 初期、入力エラー、送信中、成功、失敗状態を実装する。
- 送信中に二重送信を防ぐ。
- Worker URLを環境変数で設定する。
- API失敗時に再試行方法を表示する。

Worker作業：

- JSONリクエストを検証する。
- 許可オリジン以外のCORS要求を拒否する。
- 入力値をサーバー側でも検証する。
- レート制限またはスパム対策を追加する。
- 成功、入力エラー、レート制限、サーバーエラーをJSONで返す。
- 問い合わせ内容を不要に恒久保存しない。

完了条件：実API、入力エラー、Workerエラー、ネットワークエラーを画面上で判別できる。

### Phase 8：アクセシビリティ、SEO、性能対応

目的：見た目とモーションに依存しない公開品質を確保する。

作業内容：

- 見出し階層、ランドマーク、フォームラベルを確認する。
- キーボード操作とフォーカスリングを実装する。
- 色コントラストを確認する。
- 画像のalt、装飾要素のaria設定を確認する。
- title、description、canonical、OGPを設定する。
- `lang="ja"`、`robots.txt`、必要に応じたサイトマップを設定する。
- 画像・3D・動画の遅延読み込みを確認する。
- レイアウトシフトと不要な初期JavaScriptを確認する。

完了条件：モーションを無効にしても主要導線をキーボードで完了できる。

### Phase 9：テストとブラウザ確認

目的：受け入れ条件を検証し、公開前の崩れを修正する。

自動テスト：

- 作品データの必須項目
- 作品一覧と詳細ルーティング
- 存在しないslug
- フォーム入力検証
- 送信中・成功・失敗状態
- reduced motion分岐
- WebGLフォールバック

Playwright確認：

- ホームの初回表示
- ページ内ナビゲーション
- 作品一覧から詳細への遷移
- 外部リンク
- フォーム送信とエラー表示
- デスクトップ、タブレット、モバイル
- キーボード操作
- reduced motion
- WebGL非対応フォールバック
- コンソールエラー

視覚確認：

- デスクトップとモバイルのスクリーンショットを取得する。
- 固定カード、過剰な発光、意味のない常時ループがないか確認する。
- 長い文章、画像未設定、任意項目なしの作品で崩れないことを確認する。

完了条件：自動テストと主要ブラウザ確認が成功し、未確認事項を記録できている。

### Phase 10：公開準備とデプロイ

目的：GitHub Pagesへ安全に公開する。

作業内容：

- 本番環境変数とWorker URLを設定する。
- 本番ビルドを実行する。
- GitHub Pagesの公開設定と独自ドメインを確認する。
- デプロイ後のトップページ、作品詳細、フォーム、外部リンクを確認する。
- 404、CNAME、robots.txt、OGPを確認する。
- 変更内容と既知の制約をコミットメッセージ・報告へまとめる。

完了条件：本番URLで受け入れ条件を再確認し、重大なコンソールエラーがない。

## 4. 依存関係と実施順序

```text
Phase 0
  ↓
Phase 1 ──→ Phase 2
              ↓
           Phase 3 ──→ Phase 4
              ↓           ↓
           Phase 5 ──→ Phase 6
              ↓           ↓
           Phase 7 ───────┘
              ↓
           Phase 8
              ↓
           Phase 9
              ↓
           Phase 10
```

- Phase 1が完了するまで画面実装を開始しない。
- Phase 2のデータ型とルーティングが固まるまで作品詳細を実装しない。
- Phase 3の静的UIが完成するまでモーションを追加しない。
- Phase 5の2Dモーションを確認してから3Dを追加する。
- 問い合わせWorkerの契約が確定するまで送信処理を本番仕様にしない。
- Phase 8〜9は全画面実装後にまとめて行うが、アクセシビリティ要件は各Phaseで同時に確認する。

## 5. 変更対象の想定

### 新規または再構成する領域

- React/TypeScriptのアプリ入口
- ページ・セクション・共通コンポーネント
- プロフィール・作品データ
- デザイントークンとグローバルスタイル
- モーションフックと3Dフォールバック
- テスト設定とブラウザテスト

### 既存のまま維持する領域

- 独自ドメイン設定
- GitHub Pages運用に必要な設定
- 既存Cloudflare Workerの契約と設定（必要な修正を除く）
- ユーザーが既に行っている未コミット変更

## 6. リスクと対策

| リスク | 影響 | 対策 |
| --- | --- | --- |
| 3Dで初期表示が重くなる | 高 | 遅延読み込み、軽量アセット、静止画フォールバックを使う |
| GSAPとReactのライフサイクル不整合 | 中 | `useGSAP`とスコープ付きクリーンアップを使う |
| モーションで操作性が低下する | 高 | reduced motion、モーション非依存の操作、モバイル簡略化を実装する |
| 作品データ不足で画面設計が決まらない | 中 | Phase 0で最低3作品の仮データと最終コンテンツを確定する |
| WorkerのCORS・レート制限不備 | 高 | 許可オリジン、入力検証、レート制限、失敗テストを先に定義する |
| GitHub Pagesのパス設定不備 | 中 | 本番相当のベースパスでビルド・リンク検証を行う |
| 既存削除変更との競合 | 高 | 作業対象を新規実装領域に限定し、変更前後でgit diffを確認する |

## 7. 作業時の品質ルール

- 対象範囲を超える機能を追加しない。
- 既存のユーザー変更を復元・削除・上書きしない。
- 新規コメントが必要な場合は先頭に`[AI]`を付ける。
- 既存ファイルを変更する場合は、変更前にインデントと命名規約を確認する。
- 各Phase終了時に型検査、関連テスト、差分確認を行う。
- UI変更時はデスクトップとモバイルを確認する。
- 動きの追加前に静的な情報階層が成立していることを確認する。
- 問題が解決できない場合は、原因と未完了事項を`ISSUE.md`または所定のIssue文書へ記録する。

## 8. 最終チェックリスト

- [ ] 実装前のコンテンツ・URL・Worker設定が確定している
- [ ] TypeScriptで起動・型検査・ビルドできる
- [ ] ホームの全セクションが表示される
- [ ] 作品一覧と作品詳細がデータ駆動で動作する
- [ ] 404相当の作品表示がある
- [ ] 2Dモーションが主要情報を補助している
- [ ] reduced motionに対応している
- [ ] 3Dフォールバックがある
- [ ] 問い合わせフォームの全状態がある
- [ ] WorkerのCORS・入力検証・スパム対策がある
- [ ] キーボード操作とフォーカス表示を確認した
- [ ] SEOメタデータと独自ドメインを確認した
- [ ] デスクトップ・タブレット・モバイルを確認した
- [ ] Playwrightと単体テストが成功した
- [ ] 本番ビルドとGitHub Pages公開を確認した
- [ ] 変更差分に意図しないファイルが含まれていない


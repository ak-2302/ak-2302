const params=new URLSearchParams(location.search);const root=document.documentElement;root.style.setProperty("--color",params.get("color")||"#f6f7f1");root.style.setProperty("--accent",params.get("accent")||"#b8f525");root.style.setProperty("--bg",params.get("bg")||"#101211");root.style.setProperty("--shadow",`${params.get("shadow")||12}px`);const outline=params.get("outline")||0;root.style.setProperty("--stroke",`-${outline}px -${outline}px 0 #000,${outline}px -${outline}px 0 #000,-${outline}px ${outline}px 0 #000,${outline}px ${outline}px 0 #000`);const total=Math.max(0,(Number(params.get("minutes"))||0)*60+(Number(params.get("seconds"))||0));let remaining=total;let timerId=null;const output=document.querySelector("#time");const bar=document.querySelector("#bar");function render(){const hours=Math.floor(remaining/3600);const minutes=Math.floor(remaining%3600/60);const seconds=remaining%60;const format=params.get("format")||"auto";output.textContent=(format==="hms"||format==="auto"&&hours>0)?`${String(hours).padStart(2,"0")}:${String(minutes).padStart(2,"0")}:${String(seconds).padStart(2,"0")}`:`${String(Math.floor(remaining/60)).padStart(2,"0")}:${String(seconds).padStart(2,"0")}`;bar.style.width=`${total?remaining/total*100:0}%`}function start(){if(timerId||remaining<=0)return;timerId=setInterval(()=>{remaining-=1;render();if(remaining<=0)pause()},1000)}function pause(){clearInterval(timerId);timerId=null}function reset(){pause();remaining=total;render()}document.querySelector("#start").addEventListener("click",start);document.querySelector("#pause").addEventListener("click",pause);document.querySelector("#reset").addEventListener("click",reset);render();

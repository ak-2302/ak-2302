const params=new URLSearchParams(location.search);const root=document.documentElement;const format=params.get("format")||"hms";root.style.setProperty("--color",params.get("color")||"#f6f7f1");root.style.setProperty("--accent",params.get("accent")||"#b8f525");root.style.setProperty("--bg",params.get("bg")||"#101211");root.style.setProperty("--shadow",`${params.get("shadow")||16}px`);const outline=params.get("outline")||0;root.style.setProperty("--stroke",`-${outline}px -${outline}px 0 #000,${outline}px -${outline}px 0 #000,-${outline}px ${outline}px 0 #000,${outline}px ${outline}px 0 #000`);document.body.className=format;const timeElement=document.querySelector("#time");const secondsElement=document.querySelector("#seconds");const dateElement=document.querySelector("#date");function tick(){const now=new Date();timeElement.textContent=now.toLocaleTimeString("ja-JP",{hour:"2-digit",minute:"2-digit",hour12:false});secondsElement.textContent=String(now.getSeconds()).padStart(2,"0");dateElement.textContent=now.toLocaleDateString("ja-JP",{month:"2-digit",day:"2-digit",weekday:"short"})}tick();setInterval(tick,250);
